{% extends 'baseAdmin.html.twig' %}

{% block title %}New Guide{% endblock %}
 
{% block body %}
<style>
 .button-update {
  background-color: blue;
  color: white; 
  padding: 5px 10px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}
</style>
    <div class="container" style="margin-top: 200px;">

    <h1>Create New Guide</h1>
 {% if form is defined %}
            {{ form_start(form, { 'attr':{'novalidate':'novalidate'  }}) }}



{{ form_end(form) }}
        {% endif %}
            <a href="{{ path('app_guide_new', {'id': guide.id}) }}" class="button-submit">submit</a>
            <a href="{{ path('app_guide_index') }}" class="button-submit">Back to Guide List</a>

    </a>
    </div>

{% endblock %}